import * as React from 'react';
import { Trans } from 'react-i18next';

import '@src/app.scss';

export const App = () => {
  return (
    <div className="app">
      <Trans>app.welcome</Trans>
    </div>
  )
};
